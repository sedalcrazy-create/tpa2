# Simple Dockerfile - uses pre-existing nginx image
FROM nginx:1.25-alpine

# Copy pre-built dist files (built locally)
COPY dist /usr/share/nginx/html/tpa

# Copy nginx config
COPY nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
